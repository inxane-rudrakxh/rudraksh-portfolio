// Framer editor stub — removed for self-hosted deployment.
// The original init.mjs imported from https://app.framerstatic.com which is
// only needed for Framer's in-browser editor. Regular visitors never trigger
// this code path, but the module is replaced here to ensure the site works
// fully offline with no external dependencies on Framer servers.
export function createEditorBar() { return null; }
